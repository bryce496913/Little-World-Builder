# New app-ready island assets are stored as Git LFS pointers so code review
# systems do not need to embed binary USDZ archive data in patches.
App[[:space:]]Ready[[:space:]]USDZ/*_island.usdz filter=lfs diff=lfs merge=lfs -text
